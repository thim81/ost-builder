# OST Builder CLI

Run locally via npx:

```bash
npx ost-builder <file.md> [options]
```

Options:

- `--format <json|markdown>`
- `--pretty`
- `--share`
- `--share-base <url>`
- `--name <name>`
- `--help, -h`
